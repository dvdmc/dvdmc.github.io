---
const { title, company, location, startDate, endDate, description } = Astro.props;
---
<div class="animate-fade-in-left max-w-3xs sm:max-w-md">
  <h3 class="font-bold text-base sm:text-lg">{startDate} - {endDate}</h3>
  <h4 class="pr-4 font-semibold text-base sm:text-lg break-words">{title}</h4>
  <p class="pr-4 font-light italic text-sm md:text-base break-words">{company}</p>
  <p class="pr-4 text-sm sm:text-base">{location}</p>
  <p class="pr-4 text-sm sm:text-base">{description}</p>
</div>
